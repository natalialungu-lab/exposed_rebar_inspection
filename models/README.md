# Model checkpoint

The selected trained YOLOv8 segmentation checkpoint is `best.pt`.

It was trained for 30 epochs using `yolov8n-seg.pt`, image size 640, batch size 8, a Tesla T4 GPU, and random seed 42.

The model detects and segments exposed reinforcement bars in concrete images.
